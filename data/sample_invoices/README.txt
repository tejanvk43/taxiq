Sample invoice images (`invoice_1.jpg`..`invoice_3.jpg`) are generated automatically on first backend start.

Reason: this repo is kept text-only for tooling compatibility, so binary JPGs are created at runtime using Pillow.

